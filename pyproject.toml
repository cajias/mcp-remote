[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mpc_remote"
version = "0.1.0"
description = "ZeroMQ implementation of the Model Context Protocol"
readme = "README.md"
authors = [
  {name = "MPC Remote Contributors"}
]
requires-python = ">=3.8"
license = {text = "MIT"}
dependencies = [
    "pyzmq>=25.1.2",
    "jsonschema>=4.0.0",
    "cryptography>=3.4.0",
    "anyio>=4.0.0",
    "pydantic>=2.0.0",
    "websockets>=14.0",
    # Pydantic evaluates `X | None` field annotations at runtime; on Python
    # < 3.10 the `|` operator on types is unsupported, so it needs this backport.
    "eval-type-backport; python_version < '3.10'"
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.1.0",
    "ruff>=0.3.5",
    # Cognito authentication feature deps (exercised by tests/test_cognito_auth.py).
    # These really belong in a dedicated `cognito` extra; placed here so CI can run the tests.
    "boto3>=1.34.0",
    "PyJWT>=2.8.0"
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
# Configure asyncio handling
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"

# Test discovery paths
testpaths = ["tests"]
python_files = ["test_*.py"]

# Test execution options
addopts = [
    "-v",                 # Verbose output
    "--cov=mpc_remote",   # Enable coverage for our package
    "--cov-report=term",  # Show coverage in terminal
    "--cov-report=html",  # Generate HTML coverage report
]

[tool.coverage.run]
source = ["mpc_remote"]
branch = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "pass",
    "raise ImportError",
]